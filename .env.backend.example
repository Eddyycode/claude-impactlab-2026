# ============================================================
# Backend env (Dev A)
# Copiar a `.env` local y llenar los valores reales.
# `.env` está en .gitignore — NUNCA commitear secretos.
# ============================================================

# --- Anthropic (Claude API) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Supabase ---
# Proyecto "claude-impact" (ref: pcbhmmsifoycmcybwkvy)
SUPABASE_URL=https://pcbhmmsifoycmcybwkvy.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...           # legacy JWT anon
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...          # nuevo formato (opcional)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...              # Dashboard → Settings → API
# ^ solo para `data_ingestion/`, NUNCA lo uses desde el gateway

# --- Gateway FastAPI ---
GATEWAY_PORT=8000

# --- INEGI (opcional — solo si usamos denue_near) ---
INEGI_TOKEN=
